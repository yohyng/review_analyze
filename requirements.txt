# --- ingestion layer (steps 1-4) ---
streamlit>=1.30
pandas>=2.0
openpyxl>=3.1        # .xlsx reading
pytest>=8.0          # tests

# --- analysis layer (steps 5-6) ---
plotly>=5.18

# --- text analysis + LLM (steps 7-8) ---
janome>=0.4
scikit-learn>=1.4
numpy>=1.24          # 感情・トピック統合スコアモデル
requests>=2.31       # Turso HTTP / KAIZODE API / OSM・Wikidata
# Gemini API is called via requests (no extra SDK needed)

# --- report export (step 9) ---
python-pptx>=1.0
Pillow>=10.0         # 施設写真の自動リサイズ（PROFILE）
# scikit-learn>=1.4  # step 7: TF-IDF
# sudachipy>=0.6     # step 7: Japanese tokenizer
# sudachidict-core
# python-pptx>=0.6   # step 9: PowerPoint export
# anthropic>=0.39    # step 8: LLM insights
