# VoiceBAUM — runtime + test dependencies.
#
# Versions are PINNED to the exact set the test suite (142 tests) passes on, so
# a Streamlit Cloud rebuild installs the same versions every time instead of
# silently resolving newer, possibly-incompatible releases. (Unbounded ">="
# used to let a major upgrade break the deploy — e.g. Python 3.14 / a new
# pandas.) Python 3.12 is the target runtime (Streamlit Cloud → Settings →
# General). To upgrade a package, bump it here deliberately and re-run
# `python -m pytest -q`.

# --- app framework / data ---
streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6            # 感情・トピック統合スコアモデル

# --- ingestion ---
openpyxl==3.1.5         # .xlsx reading

# --- analysis ---
plotly==6.8.0
janome==0.5.0
scikit-learn==1.9.0
scipy==1.17.1           # NMSI/VFFI のクラスタ対応づけ（linear_sum_assignment）

# --- NMSI / VFFI（src/nmsi・外部プロジェクトからの移植）---
#   22観点スコアと違い、こちらは**生成AIを使う**。文ごとの属性付けを
#   OpenAI 互換 API に投げるので、分析のたびに課金が乗る。
pydantic==2.13.5        # LLM の構造化出力スキーマ
openai==3.10.0           # OpenAI 互換 API クライアント

# --- integrations (Turso HTTP / KAIZODE API / OSM・Wikidata) ---
requests==2.33.1
# Gemini API is called via requests (no extra SDK needed)

# --- report export ---
python-pptx==1.0.2
Pillow==12.3.0          # 施設写真の自動リサイズ（PROFILE）

# --- tests / CI ---
pytest==9.1.1
